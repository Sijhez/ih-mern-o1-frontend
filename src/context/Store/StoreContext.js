import { createContext } from "react";

const StoreContext = createContext(null)  //aqui vamos a generar variables globales. Sustituye al PROPS que usaubamos anteriormente

export default StoreContext